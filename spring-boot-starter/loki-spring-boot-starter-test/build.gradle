dependencies {
    api project(":spring-boot-starter:loki-spring-boot-starter")

    testImplementation("org.springframework.boot:spring-boot-starter-web:${springBootVersion}")
    testImplementation("${lib.'fastjson2'}")
}
