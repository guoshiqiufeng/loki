dependencies {
    api project(":loki-annotation")
    api "${lib.'fastjson2'}"
    api "${lib.'spring-context'}"
    implementation "${lib.'rocketmq-client-java'}"
    implementation "${lib.'spring-kafka'}"
    implementation "${lib."slf4j-api"}"
}